{
  "name": "thinking-of-you",
  "version": "1.0.0",
  "description": "Tap a button, your partner gets a push notification.",
  "main": "server.js",
  "engines": {
    "node": ">=20.19.0"
  },
  "scripts": {
    "start": "node server.js",
    "build": "cd client && npm install && npm run build",
    "genkeys": "web-push generate-vapid-keys"
  },
  "dependencies": {
    "cloudinary": "^2.10.0",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "mongodb": "^7.5.0",
    "multer": "^2.0.0",
    "socket.io": "^4.7.5",
    "web-push": "^3.6.7"
  },
  "devDependencies": {
    "puppeteer-core": "^25.3.0",
    "socket.io-client": "^4.8.3"
  }
}
